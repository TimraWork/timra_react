import React from "react";
import { LoginDetails } from "../helpers/item-details";

const LoginPage = ({ isLoggedIn, onLogin }) => {
    // console.log(onLogin);
    return <LoginDetails />;
};

export default LoginPage;
