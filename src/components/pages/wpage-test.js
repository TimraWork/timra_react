import React, { Component } from "react";

import withPage from "../../hoc/with-page";

class TestPage extends Component {
    render() {
        return <React.Fragment>asfdds</React.Fragment>;
    }
}

export default withPage(TestPage);
